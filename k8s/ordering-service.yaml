apiVersion: v1
kind: Service
metadata:
  name: ordering-service
  namespace: eshop
  labels:
    app: ordering-api

spec:
  type: ClusterIP

  selector:
    app: ordering-api

  ports:
    - name: http
      port: 80
      targetPort: 80
